<section class="inner">
	<% if params[:customer_id] %>
		<h2 class="section-ttl"><%= @customer.full_name %>さんの注文履歴一覧</h2>
	<% else %>
		<h2 class="section-ttl">注文履歴一覧</h2>
	<% end %>
	<table class="admin_orders has_border">
		<thead>
			<tr>
				<th>注文日</th>
				<th>購入者</th>
				<th>注文個数</th>
				<th>総計</th>
				<th>注文ステータス</th>
			</tr>
		</thead>
		<tbody>
			<% @orders.each do |order| %>
				<tr>
					<td><%= link_to order.created_at_jst.strftime("%Y/%m/%d %H:%M:%S"), admin_order_path(order), class: "underline" %></td>
					<td><%= link_to order.customer.full_name, admin_customer_path(order.customer) %></td>
					<td><%= order.order_details.sum(:amount) %></td>
					<td><%= order.grand_total %>円</td>
					<td class="text-center"><%= order.status_i18n %></td>
				</tr>
			<% end %>
		</tbody>
	</table>
	<%= paginate @orders %>
</section>